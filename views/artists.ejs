
  <!DOCTYPE html>
  <html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= artist %> - Works</title>
    <link rel="stylesheet" type="text/css" href="/style.css" />
  </head>
  <body>

    <h6>The Artist API</h6>
    <div class="bg">
    <h1><%= artist %></h1>
     
    <h2>Works:</h2>
    <% if (works.length > 0) { %>
      <ul>
        <% works.forEach(work => { %>
          <li>
            <h3><%= work.title %>  (<%= work.year %>)</h3>
            <p><%= work.medium %></p>
            <h4><img src="/images/<%= work.image %>" alt="<%= work.title %>" /></h4>
          </li>
        <% }) %>
      </ul>
    <% } else { %>
      <p>No works available for this artist.</p>
    <% } %>
  
    <h2>Details:</h2>
    <% if (details.length > 0) { %>
      <ul>
        <% details.forEach(detail => { %>
          <li>
            <%= detail.artistName %><br>  <%= detail.born %>
              - <%= detail.died %><br>  <%= detail.activeYears %><br>
               <%= detail.nationality %><br>  <%= detail.artMovement %><br>
               <%= detail.field %><br>  
               <a href="<%= detail.wikipedia %>"><%= detail.wikipedia %></a>                          
          </li>
        <% }) %>
      </ul>

    <% } else { %>
      <p>No details found for this artist.</p>
    <% } %>

    <h5>Join the Mailing List</h5>
    <div2 class="form-container">
      <form action="/submit" method="POST">
        <label for="name">Name:</label>
        <input type="text" id="name" name="name" required><br><br>
        <label for="email">Email:</label>
        <input type="email" id="email" name="email" required><br><br>
        <button type="submit" class="move2">Submit</button>
      </form>
    </div2>
    </div>
    <br>
    <br>
    <br>

  </body>
  </html>
  
